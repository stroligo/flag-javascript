{
  "compilerOptions": {
    "outDir": "./dist", // Diretório onde os arquivos .js serão gerados
    "rootDir": "./src", // Diretório raiz para o código e testes
    "target": "es2016", // Versão do JavaScript de saída
    "module": "commonjs" // Sistema de módulos
  },
  "include": ["src/**/*"], // Inclua todos os arquivos .ts no diretório src
  "exclude": ["node_modules"] // Exclua a pasta node_modules
}
